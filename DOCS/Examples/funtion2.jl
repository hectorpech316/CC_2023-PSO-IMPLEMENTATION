function function2(x)
    """
    This function calculates the result of
    evaluate x -> 3*cos(x) + (sin(x))^2
   The minimum of this function in the
    interval  [0,6] is -2 in x = pi
    """
    return 3*cos(x) + (sin(x))^2
end
